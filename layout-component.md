# 画面設計書: コンポーネント一覧（UI 部品カタログ）

| 項目 | 内容 |
|---|---|
| ルート | `/layout-component/` |
| 実装ファイル | `app/layout-component/page.tsx` |
| メニュー導線 | ⚪なし（開発用の UI 部品カタログ。`components/Sidebar.tsx` に導線は無い。`Header`/`Sidebar`/`Footer` も描画しない独立ページ） |
| サイドバー表記 | — |
| キャプチャ条件 | パッケージ・frame・年度に依存しない静的カタログ（データ取得なし）。基準環境 package_id=43 のセッションで撮影 |
| 関連 ADR / 設計 | ADR-F085（パンくずは `AppBreadcrumb`＋レジストリで自動生成・本ページはサンプルのため手書き）。`[要協議]` コメント多数（旧 Bootstrap 部品→`@gyosei-team/shared-components` の対応付け検討メモ） |

## 1. 画面概要
`@gyosei-team/shared-components` の UI 部品（ボタン各種・タグ・ステータスフロー・タブ・入力/選択/日付・ラジオ/チェック・テーブル・ページネーション・モーダル）の見た目とバリアントを一覧するための開発用カタログ画面である。SC 移行（Bootstrap 置換）で採用する部品と、対応未確定の箇所（`[要協議]`）を並べて確認する用途であり、業務ロジックやデータ取得は持たない。

## 2. ルート / URL クエリパラメータと挙動
| パラメータ | 必須 | 例 | 挙動 |
|---|---|---|---|
| （なし） | — | — | URL クエリパラメータは一切参照しない。常に同一の静的カタログを描画する。 |

- 未指定/不正値時の分岐は無い（クエリ非依存・データ取得なし）。

## 3. 主要 UI 要素の動作
| UI 要素 | 種別 | 動作 | 遷移先 / 呼び出し |
|---|---|---|---|
| パンくず | Breadcrumb | 見た目サンプル（手書き items）。実ページは `AppBreadcrumb`＋レジストリで生成する旨を注記表示する | — |
| 見出し（h1/h2/h3・ライン有/無） | 見出し | `c-heading-h1/h2/h3-title` の表示例 | — |
| ボタン R/S/SS・フッターボタン・ステータスボタン・非活性・アイコン付 | Button/IconButton | variant（primary/secondary/tertiary/destructive）× size（既定/sm/thin）と各アイコン付きの見た目例（操作は空ハンドラ） | — |
| トグルボタン | SegmentedControl | 5 値のトグル例（`toggleValue` を保持） | — |
| モーダルダイアログ（sm/デフォルト/lg/xl） | Dialog | 各サイズの `Dialog` を開閉するデモ（`openDialog` state で制御） | — |
| その他ボタン / 正方形ボタン | Button/IconButton | 編集・追加・検索・印刷・複製・削除等のアイコンボタン例（`[要協議]` の仮実装を含む） | — |
| タグラベル / カラータグ | Badge | 必須・リンク・削除可タグ、13 色相当のタグを近接 variant に集約した例 | — |
| 矢印フローパーツ | StatusFlow | 2〜6 ステップのステータスフロー表示例 | — |
| タブ（小/中/大/コンテンツ内） | Tabs | `TabsList` の size 違いのタブ切替デモ | — |
| 開閉見出し | CollapsibleSection | 折りたたみ見出しの開閉デモ | — |
| 入力テキスト（S/M/L・エラー・非活性） | Input/InputWithUnit | サイズ・状態別の入力例 | — |
| テキストエリア（通常/エラー/非活性） | Textarea | 状態別の複数行入力例 | — |
| セレクト（通常/エラー/非活性） | Select | 状態別のセレクト例 | — |
| カレンダー（単/範囲/非活性） | DateField/DateRangeField | 日付選択の例 | — |
| ラジオ / チェックボックス（通常/エラー/非活性） | Radio/Checkbox | 状態別の選択例 | — |
| 基本テーブル（inner / ヘッダーブルー / ヘッダーグレー） | Table | `c-table`・`gray-ver`・`stripe-line` 等のテーブルスタイル例（ヘッダ内に `DropdownMenu` ミニメニュー） | — |
| ページネーション | Pagination | `currentPage`/`totalPages` によるページ送りデモ | — |

- すべて見た目・状態の確認用であり、確認ダイアログ・トースト通知・API 呼び出しは無い（Dialog も開閉デモのみ）。
- `[要協議]` コメントは、旧 Bootstrap 独自部品（バーコード・スタンプ・独自 SVG アイコン・13 色タグ等）を SC のどの部品/variant に寄せるか未確定である旨を示す。

## 4. API 連携
| タイミング | メソッド/エンドポイント | 用途 |
|---|---|---|
| （なし） | — | このページは API を呼び出さない（静的カタログ） |

（呼び出し API が存在しないため openapi 照合の対象は無い。）

## 5. 状態遷移 / 前提条件
- 業務状態を持たない開発用カタログであり、前提データ・ステータス遷移・他画面との順序依存は無い。
- ローカル state はダイアログ開閉・タブ・各入力の見た目デモの保持にのみ使う。

## 6. スクリーンショット
- 初期表示: `../screenshots/layout-component/initial.png`
